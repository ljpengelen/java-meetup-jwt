---

spring:
  datasource:
    url: jdbc:postgresql://java-meetup-postgres:5432/java_meetup
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
csrf:
  enable_check: true
  target: "http://localhost:8000/"

---

spring:
  profiles: test
  datasource:
    url: jdbc:postgresql://java-meetup-postgres:5432/java_meetup_test
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop

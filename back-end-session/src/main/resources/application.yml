---

spring:
  datasource:
    url: "jdbc:h2:file:./db"
  jpa:
    hibernate:
      ddl-auto: create
csrf:
  enable_check: true
  target: "http://localhost:8000/"

---

spring:
  profiles: test
  datasource:
    url: "jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE"
  jpa:
    hibernate:
      ddl-auto: create-drop
